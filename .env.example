# IPFS/Kubo Configuration
# No environment variables needed for basic setup
# IPFS runs with default configuration

# Optional: Set custom IPFS profile
# IPFS_PROFILE=server

# Optional: Limit file descriptors
# IPFS_FD_MAX=4096
